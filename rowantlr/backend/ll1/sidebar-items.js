initSidebarItems({"fn":[["append_first_of","Append `FIRST(β)` to a given [`BTreeSet`] for any string `β ∈ V*`, according to the provided `FIRST` and `DEDUCE_TO_EMPTY` sets. The returned `bool` indicates whether or not this input string may deduce to empty."],["calc_deduce_to_empty","Calculate the `DEDUCE_TO_EMPTY` set for each non-terminal."],["calc_first","Calculate the `FIRST` set for each non-terminal."],["first_of","Calculate `FIRST(β)` for any string `β ∈ V*`, according to the provided `FIRST` and `DEDUCE_TO_EMPTY` sets. The `bool` indicates whether or not this string may deduce to empty."]],"struct":[["Lookahead","A lookahead token."]]});